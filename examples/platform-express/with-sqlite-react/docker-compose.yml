services:
  backend:
    build:
      context: ../../..
      dockerfile: examples/platform-express/with-sqlite/Dockerfile
    image: zacatl-express-sqlite
    container_name: express-sqlite-backend
    ports:
      - "8181:8181"
    environment:
      - NODE_ENV=production
      - PORT=8181
      - DATABASE_URL=sqlite:database.sqlite
    volumes:
      # Persist SQLite database
      - ./data:/app/data
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://localhost:8181/greetings').catch(() => process.exit(1))",
        ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s
    restart: unless-stopped

  # frontend:
  #   build:
  #     context: ../../..
  #     dockerfile: examples/platform-express/with-sqlite/apps/frontend/Dockerfile
  #   image: zacatl-express-sqlite-frontend
  #   container_name: express-sqlite-frontend
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - VITE_API_URL=http://localhost:8082
  #   depends_on:
  #     backend:
  #       condition: service_healthy
  #   restart: unless-stopped

volumes:
  data:
