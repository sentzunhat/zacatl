{
  "name": "zacatl-fastify-postgres-backend",
  "version": "1.0.0",
  "description": "Fastify + PostgreSQL (Sequelize) backend using Zacatl framework",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "start:dev": "tsx src/index.ts",
    "dev": "tsx --watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist database.sqlite"
  },
  "dependencies": {
    "pg": "^8.11.5"
  }
}
