{
  "name": "mongodb-example-backend",
  "version": "1.0.0",
  "description": "Fastify + MongoDB backend using Zacatl framework",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "start:dev": "tsx src/index.ts",
    "dev": "tsx --watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "fastify": "^5.0.0",
    "mongoose": "^9.0.0",
    "reflect-metadata": "^0.2.2",
    "tsyringe": "^4.10.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.16",
    "typescript": "^5.3.3"
  }
}
