services:
  postgres:
    extends:
      file: ../compose/databases/postgres.yml
      service: postgres
    container_name: express-postgres-db
    ports:
      - '${EXPRESS_POSTGRES_HOST_PORT:-5434}:5432'

  backend:
    build:
      context: ../..
      dockerfile: examples/Dockerfile
      args:
        EXAMPLE: express-postgres-react
        DATABASE_DRIVER: pg
        BACKEND: apps/backend
        FRONTEND: apps/frontend
        PORT: 8183
    image: zacatl-express-postgres
    container_name: express-postgres-backend
    ports:
      - '8183:8183'
    environment:
      - NODE_ENV=production
      - PORT=8183
      - DATABASE_URL=postgres://local:local@postgres:5432/appdb
      - HEALTHCHECK_PATH=/api/greetings
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test:
        [
          'CMD',
          '/nodejs/bin/node',
          '-e',
          "const path=process.env.HEALTHCHECK_PATH||'/greetings'; const url=`http://127.0.0.1:$${process.env.PORT || '8183'}$${path}`; const controller=new AbortController(); const timer=setTimeout(() => controller.abort(), 4000); fetch(url, { signal: controller.signal }).then((r) => { clearTimeout(timer); if (!r.ok) process.exit(1); }).catch(() => process.exit(1));",
        ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s
    restart: unless-stopped

volumes:
  postgres-data:
