{
  "name": "zacatl-fastify-mongodb-backend",
  "version": "1.0.0",
  "description": "Fastify + MongoDB backend using Zacatl framework",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "build": "tsc && npx tsx ../node_modules/@sentzunhat/zacatl/scripts/build/fix-esm.ts dist",
    "clean": "rm -rf dist"
  },
  "devDependencies": {}
}
