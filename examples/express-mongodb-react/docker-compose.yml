services:
  mongodb:
    extends:
      file: ../compose/databases/mongodb.yml
      service: mongodb
    container_name: express-mongodb-db
    ports:
      - '${EXPRESS_MONGODB_HOST_PORT:-27018}:27017'
    volumes:
      - ./mongo-init.js:/docker-entrypoint-initdb.d/mongo-init.js:ro

  backend:
    build:
      context: ../..
      dockerfile: examples/Dockerfile
      args:
        EXAMPLE: express-mongodb-react
        DATABASE_DRIVER: mongoose
        BACKEND: apps/backend
        FRONTEND: apps/frontend
        PORT: 8182
    image: zacatl-express-mongodb
    container_name: express-mongodb-backend
    ports:
      - '8182:8182'
    environment:
      - NODE_ENV=production
      - PORT=8182
      - MONGO_URI=mongodb://local:local@mongodb:27017/appdb
      - HEALTHCHECK_PATH=/api/greetings
    depends_on:
      mongodb:
        condition: service_healthy
    healthcheck:
      test:
        [
          'CMD',
          '/nodejs/bin/node',
          '-e',
          "const path=process.env.HEALTHCHECK_PATH||'/greetings'; const url=`http://127.0.0.1:$${process.env.PORT || '8182'}$${path}`; const controller=new AbortController(); const timer=setTimeout(() => controller.abort(), 4000); fetch(url, { signal: controller.signal }).then((r) => { clearTimeout(timer); if (!r.ok) process.exit(1); }).catch(() => process.exit(1));",
        ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s
    restart: unless-stopped

volumes:
  mongo-data:
