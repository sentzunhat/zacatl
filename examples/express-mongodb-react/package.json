{
  "name": "zacatl-express-mongodb-monorepo",
  "version": "1.0.0",
  "description": "Full-stack monorepo: Express + MongoDB backend with React frontend",
  "private": true,
  "type": "module",
  "packageManager": "npm@11.18.0",
  "engines": {
    "node": ">=26.3.0",
    "npm": ">=11.0.0"
  },
  "workspaces": [
    "apps/backend",
    "apps/frontend"
  ],
  "scripts": {
    "dev": "npm run backend:dev & npm run frontend:dev",
    "backend:dev": "npm --prefix apps/backend run dev",
    "frontend:dev": "npm --prefix apps/frontend run dev",
    "build": "npm run backend:build && npm run frontend:build",
    "backend:build": "npm --prefix apps/backend run build",
    "frontend:build": "npm --prefix apps/frontend run build",
    "start": "npm --prefix apps/backend run start",
    "clean": "rm -rf apps/*/dist apps/*/node_modules"
  },
  "dependencies": {
    "@sentzunhat/zacatl": "file:../../",
    "mongoose": "^9.7.4"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^6.0.3"
  }
}
