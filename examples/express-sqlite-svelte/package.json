{
  "name": "zacatl-express-sqlite-svelte-monorepo",
  "version": "1.0.0",
  "description": "Full-stack monorepo: Express + SQLite backend with Svelte frontend",
  "private": true,
  "type": "module",
  "packageManager": "npm@11.17.0",
  "engines": {
    "node": ">=26.3.0",
    "npm": ">=11.0.0"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "backend:dev": "npm --prefix backend run dev",
    "backend:start": "npm --prefix backend run start",
    "backend:build": "npm --prefix backend run build",
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "frontend:preview": "npm --prefix frontend run preview",
    "dev": "npm run backend:dev & npm run frontend:dev",
    "build": "npm run backend:build && npm run frontend:build",
    "start": "npm run backend:start",
    "clean": "rm -rf apps/*/dist apps/*/node_modules"
  },
  "dependencies": {
    "@sentzunhat/zacatl": "file:../../"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^6.0.3"
  }
}
