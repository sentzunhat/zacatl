# Frontend Configuration

# API base URL (typically points to backend server)
# For local development: http://localhost:8081
# For production: https://your-api.example.com
VITE_API_BASE=http://localhost:8081

# Optional API token for authentication (leave empty if not needed)
VITE_API_TOKEN=
