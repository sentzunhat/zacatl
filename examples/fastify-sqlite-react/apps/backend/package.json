{
  "name": "zacatl-fastify-sqlite-backend",
  "version": "1.0.0",
  "description": "Fastify + SQLite (Sequelize) backend using Zacatl framework",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "npm run build && npm run start",
    "build": "rm -rf dist && npm --prefix ../.. exec -- tsc -p tsconfig.json && node ../../../../build-scripts-esm/build/fix-esm.js dist",
    "test": "vitest run",
    "clean": "rm -rf dist ../../database.sqlite"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
