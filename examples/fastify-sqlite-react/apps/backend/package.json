{
  "name": "zacatl-fastify-sqlite-backend",
  "version": "1.0.0",
  "description": "Fastify + SQLite (Sequelize) backend using Zacatl framework",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "npm run build && npm run start",
    "build": "npm --prefix ../.. exec -- tsc -p tsconfig.json && node ../../../../build-scripts-esm/build/fix-esm.js dist",
    "clean": "rm -rf dist ../../database.sqlite"
  },
  "devDependencies": {}
}
