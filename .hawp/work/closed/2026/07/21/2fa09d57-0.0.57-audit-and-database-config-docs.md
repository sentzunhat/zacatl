# 2fa09d57 — 0.0.57 optional peer audit behavior and DatabaseConfig migration clarity

input: |
Clarify Zacatl 0.0.57 follow-up behavior: Sequelize audit findings belong to
SQL consumers that install Sequelize explicitly; non-SQL consumers should not
install Sequelize; node:sqlite avoids external sqlite3 and Sequelize; and the
DatabaseConfig migration from connectionString to connection.url should be
obvious.

context: |
0.0.57 moved database ecosystems to optional peers and introduced named
database connections. Consumers upgrading from 0.0.56 need crisp docs for
audit interpretation and config migration.

mission: |
Update release-facing docs and public type comments so the 0.0.57 audit and
DatabaseConfig migration story is clear.

constraints: |
Preserve existing release scope and do not change runtime behavior. Keep SQL
adapters available while making SQL-owned audit surface explicit.

output: |
README, migration guide, changelog, service docs, and type comments updated;
follow-up implementation items recorded in HAWP.

## Direct Verification

- `git diff --check` passed.
- `npm run type:check:src` passed.
- `npm audit --omit=dev` passed with `found 0 vulnerabilities`.
- Public docs were swept for stale `Node 22.5` native SQLite guidance and
  updated to Zacatl's Node 26 runtime contract.
