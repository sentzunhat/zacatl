# 57a11d0c — 0.0.57 consumer audit migration docs

input: |
  Apply the Zacatl-side fixes from the on-demand-infra audit context for the
  0.0.57 release, then update detailed changelog and migration documentation.

context: |
  on-demand-infra on Zacatl 0.0.56 inherited the Sequelize -> uuid@8 audit
  chain even when the app did not intentionally use Sequelize. The current
  Zacatl 0.0.57 work moves database ecosystems to optional peers, keeps
  Sequelize only in dev/example SQL paths, and verifies Docker example runtime.

mission: |
  Make the 0.0.57 consumer upgrade path explicit and verify the published
  package shape no longer installs Sequelize for non-SQL consumers.

constraints: |
  Keep package version at 0.0.57. Do not downgrade Zacatl or force a nested
  Sequelize uuid override. Preserve non-root Docker runtime behavior and keep
  docs aligned with the factored example compose defaults.

output: |
  Updated changelog, migration guide, docs drift fixes, and command evidence
  for npm audit/publish shape checks.

## Closure

Status: done

Directly verified:

- `npm audit --omit=dev` passed under Node 26.
- `npm run prepublish:guard` passed and confirmed 0.0.57 is not published yet.
- `npm run prepare-publish` produced a staged package with `sequelize` only as
  an optional peer, not a runtime dependency.
- A packed 0.0.57 tarball installed into a temp consumer with `uuid@14.0.1`, no
  Sequelize install, and `npm audit --omit=dev` clean.
- `git diff --check` passed.
