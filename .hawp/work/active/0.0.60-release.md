# 0.0.60 release candidate — CI, Docker, DI, and README consolidation

**UUID:** `0.0.60` · **Type:** release/tooling · **Priority:** P1 · **Reported:** 2026-08-15
**Status:** in-progress · **Owner:** agent (unassigned)

## Goal

Prepare the accumulated `dev` work as a `0.0.60` release candidate. This
work may be reviewed and verified on `dev`, but must not be merged, tagged, or
published until the owner explicitly approves those actions.

## Included scope

- CI orchestrator layering and pending-release gating.
- SQLite, PostgreSQL, and MongoDB example Docker smoke coverage.
- MongoDB CI health-check timing hardening.
- Layered DI child containers and Server passthrough removal.
- README positioning, badges, examples, and AI acknowledgment.
- v0.1.0 milestone and `PLATFORM-001` planning records.

## Release gates

- `package.json` and `package-lock.json` both report `0.0.60`.
- Top `docs/changelog.md` entry is `0.0.60` and describes only work present in
  this candidate.
- `npm run prepublish:guard` passes and confirms `0.0.60` is not published.
- `npm test`, `npm run type:check`, `npm run lint:silent`, and `npm run build`
  pass under Node 26/npm 11+.
- `npm run prepare-publish` and `npm run smoke:consumers` pass.
- The Docker smoke matrix passes for SQLite, PostgreSQL, and MongoDB.
- No merge, tag, npm publish, or GitHub Release occurs before owner approval.

## Explicit follow-up

`NPM-OIDC-001` is a P0 security follow-up. The current release workflow still
uses `NPM_TOKEN`; do not describe the OIDC migration as complete in release
notes or merge the release until the owner decides whether that security work
must gate `0.0.60`.

## Verification checkpoint (2026-08-15)

- Confirmed `0.0.60` is not published on npm.
- Passed: 659 tests across 78 files, source/test/script type checks, lint with
  existing warnings only, ESM/CJS build, publish staging, and all required
  packed consumer smoke fixtures.
- The refreshed production dependency graph passes `npm audit --omit=dev`
  with zero vulnerabilities. ESLint 10, TypeScript 7, and the TypeScript
  ESLint 8.65 to 8.67 peer-set update remain intentionally deferred for a
  separate compatibility work item.
- Consumer smoke passed for non-SQL, node:sqlite, Mongoose, Sequelize SQLite,
  and Sequelize Postgres. The SQL fixtures reported three moderate findings
  owned by Sequelize's nested UUID dependency; no unsafe downgrade was made.
- Not yet directly verified in this shell: the full Docker matrix under Node
  26. The available local runtime was Node 24.x, below Zacatl's declared Node
  26 release contract. Use GitHub CI or a Node 26 machine for the final Docker
  and release-candidate gate.
