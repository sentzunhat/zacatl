# Status Report

#### Intent

Prepare Zacatl `0.0.60` on `dev` while keeping merge, tag, publish, and
credential changes owner-approved actions.

#### Current State

`package.json`, `package-lock.json`, and the top changelog entry now target
`0.0.60`. A P0 npm Trusted Publishing/OIDC work item was added. The branch has
not been merged, tagged, pushed, or published by this task.

#### What Was Inspected

- Current `dev` branch, worktree, release scripts, and GitHub workflows.
- `.github/workflows/release.yml` and `docs/guidelines/ci-cd-workflow.md`.
- npm registry version state and the npm Trusted Publishing documentation.
- Release guard, build, publish staging, and packed consumer smoke commands.

#### What Changed

- Added the detailed `0.0.60` changelog entry.
- Bumped package metadata from `0.0.59` to `0.0.60`.
- Added `NPM-OIDC-001` as a P0 security plan to replace `NPM_TOKEN` with npm
  Trusted Publishing through GitHub Actions OIDC.
- Added a `0.0.60` release-candidate plan and backlog entry.
- Retained the README positioning and provider-boundary planning changes.
- Refreshed compatible runtime and development dependencies while leaving
  ESLint 10, TypeScript 7, and the TypeScript ESLint peer set deferred.
- Refreshed the lockfile transitives; the production-only audit now reports
  zero vulnerabilities.

#### What Was Directly Verified

- `prepublish:guard` passed and confirmed `0.0.60` is not published.
- 659 tests across 78 files passed.
- Source, test, and script type checks passed.
- Lint passed with existing warnings and zero errors.
- ESM/CJS builds, export synchronization, package staging, and consumer smoke
  fixtures passed.
- Non-SQL, node:sqlite, Mongoose, Sequelize SQLite, and Sequelize Postgres
  packed consumers passed. SQL fixtures reported three moderate,
  Sequelize-owned nested UUID findings.
- A clean normal `npm ci` succeeded after the dependency refresh, followed by
  a passing 659-test run, type checks, lint, and build.
- `npm audit --omit=dev` reports 0 total vulnerabilities. The packed SQL
  consumer fixtures separately report three moderate findings owned by
  Sequelize's nested UUID dependency, with no safe Zacatl-side fix available.

#### What Remains Unproven

- The local shell used Node 24.x; Zacatl requires Node 26+. The full Docker
  matrix and final release verification must run under Node 26.
- npm Trusted Publishing has not yet been configured or tested.
- The legacy `NPM_TOKEN` has not been revoked.
- No merge, tag, npm publish, or GitHub Release has occurred.

#### Constraints

- Do not merge or publish until the owner approves.
- Do not change or revoke credentials during release preparation.
- Do not downgrade Sequelize's nested UUID dependency as an audit workaround.

#### Help Wanted

Approve or challenge whether `NPM-OIDC-001` must gate `0.0.60`, and provide
approval before any merge or publish action.

#### Suggested Next Step

Run the candidate through Node 26/GitHub CI, review the staged diff, then wait
for owner approval before opening or merging a release PR.
