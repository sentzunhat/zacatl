# HAWP Absorbable Export Surface

This folder is the only approved export surface for future HAWP absorption.

Inclusion requirements:

- reviewed
- portable
- public-safe
- framework-agnostic or abstraction-first
- reusable across repositories

Use `manifest.json` as the authoritative inclusion list.
